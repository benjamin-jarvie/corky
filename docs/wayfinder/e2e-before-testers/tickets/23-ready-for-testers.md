# 23 Ready for testers and contributors

Labels: wayfinder:task (AFK, one HITL check)
Blocked by: 18

## Question

Everything a stranger needs, and nothing stale:

- README: the scope section still promises a codex32 backup and the status
  section still says the next gate is M0. Rewrite both around what is true.
  State the amended SD-card rule (ticket 04) plainly.
- ISSUES.md: record tonight's findings and closures.
- TESTING.md: the Sparrow suite runs again (ticket 16); say so.
- `image/PINS`: record the OS image sha256 from the card Ben flashed, so a
  tester can verify the same image.
- A `CONTRIBUTING.md`: the test commands, the Sparrow setup, the flash
  procedure, and the one rule main enforces, in three paragraphs.
- The two-axis review, `/mp-code-review`, on the whole map's diff, per
  TESTING.md rule 5, findings verified before they are acted on.
- Ben's HITL check: read the README as a stranger would and say what is
  missing.
