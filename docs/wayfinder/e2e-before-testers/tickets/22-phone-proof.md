# 22 Prove each phone wallet with Ben's phone

Labels: wayfinder:task (HITL)
Blocked by: 12, 19, 20, 21

## Question

For each phone wallet whose research ticket found a working path, wire the
export entry to the format it names, remove the untested mark, and run
steps 2 to 4 of ticket 18 with Ben's phone as the coordinator. A wallet
whose research found no path to hand a PSBT back stays marked untested on
the list, with one line on screen saying what it can do (receive only).

Record per wallet: the app version, the import form used, the addresses
compared, and the transaction id.
