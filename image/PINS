# Core Signer pinned build tuple — dev image v0.1 (2026-08-18)
# Every release changes this file and nothing else about the build process.
# CORRECTED 2026-09-23. This named a 2026-05-13 bookworm image, and two
# things were wrong with it: the URL is 404, because Raspberry Pi
# removes old images from downloads.raspberrypi.com, and the dev board
# was never running it. `/etc/rpi-issue` on the board says "Raspberry Pi
# reference 2026-06-18", Debian 13 trixie, built by pi-gen at commit
# ca8aeed0ae300c2a89f55ce9617d5f96a27e99e5, stage2. That is this image.
#
# THE HASH BELOW WAS COMPUTED HERE, not copied. The whole 524,875,608
# bytes were downloaded on 2026-09-23 and hashed, and the result matched
# the .sha256 the Foundation publishes beside the file.
#
# It is a weaker pin than CORE_SHA256 and the difference is worth
# knowing. Core's hash is attested by eleven GPG signatures from keys
# served by a different host. This one has no signature at all: the
# image and its hash come from the same server, so it proves you got
# what we got and proves nothing about what that is. Raspberry Pi OS
# images are also not reproducible by anyone, the Foundation included,
# so nobody can rebuild this file and compare. The pi-gen commit above
# is the nearest thing to a recipe.
OS_IMAGE_URL="https://downloads.raspberrypi.com/raspios_lite_arm64/images/raspios_lite_arm64-2026-06-19/2026-06-18-raspios-trixie-arm64-lite.img.xz"
OS_IMAGE_SHA256="acff736ca7945e3b305f07cda4abdb870910e12634991da69783611756e381b3"
OS_IMAGE_BYTES="524875608"
OS_IMAGE_PIGEN_COMMIT="ca8aeed0ae300c2a89f55ce9617d5f96a27e99e5"
CORE_VERSION="31.1"
CORE_TARBALL="bitcoin-31.1-aarch64-linux-gnu.tar.gz"
CORE_URL="https://bitcoincore.org/bin/bitcoin-core-31.1/bitcoin-31.1-aarch64-linux-gnu.tar.gz"
# Verified 2026-09-03 on the Mac, out of band from bitcoincore.org:
#   11 of 11 GPG signatures on SHA256SUMS good, 0 bad, 0 missing keys.
#   Signing keys came from the guix.sigs repo (builder-keys/), a
#   different host to the one serving the binary.
#   achow101, fanquake, Sjors and hebasto each attest this same hash
#   in their own noncodesigned.SHA256SUMS for 31.1.
CORE_SHA256="dcf1873f2208ba4f962f3398d47e154c39c0084be8f4553e05c940d0ace3d004"
# The two binaries INSIDE that tarball, so an installed device can be
# re-checked without downloading 82MB again. Extracted from the tarball
# above after it matched CORE_SHA256, on 2026-09-06. This is the hash
# image/verify-install.sh compares against on the board; the tarball hash
# can only be checked at install time, which audit A7 pointed out is the
# one moment nobody is watching.
CORE_BIN_SHA256="1b279e038691c10e30d9c99a071ff967439020c4352e8ea3b156fb6b03751f83"
CLI_BIN_SHA256="815c0969dabe4b67d180f3e0c597c995ca03fd3a65ff2b2d1b1223109f6df589"
# Re-verified 2026-09-08 by downloading CORE_URL again on the dev machine:
# the tarball and both extracted binaries matched all three hashes above.
# bitcoind is 16,451,368 bytes and bitcoin-cli 2,561,232, both already
# stripped. Those two figures are what PLAN A-12b's arithmetic rests on.
CORESIGNER_COMMIT="HEAD"                             # pin to a tag for release images
# pip's packages live in image/requirements.txt since 2026-09-08, pinned
# by sha256 and installed with --require-hashes. A version alone stops an
# accidental upgrade and stops nothing else. PIP_PINS="" stayed here for
# a while after that, empty, read by nothing: a pin that pins nothing
# reads as a pin.
# The complete list of what the signer carries, with reasons, is the README
# section "What runs on the signer"; tests/test_integrity.py enforces it as
# an import allowlist. Add nothing here without adding it there.
# --- The six packages apt adds, pinned by URL and hash ---------------
#
# MEASURED on the dev board 2026-09-23: `apt-get install -s` for the
# list provision.sh asks for reports exactly SIX packages to install,
# because the 958 the OS image already carries satisfy every
# dependency. So the whole unpinned surface of this card was six files.
#
# PINNED BY URL AND HASH, NOT BY VERSION, and the reason is specific.
# archive.raspberrypi.com/debian is a ROLLING index: it carries only
# the newest version of a package, so apt version pinning cannot reach
# an older one because the version is not in the index to pin to. There
# is no snapshot.raspberrypi.com. deb.debian.org keeps its pool, and
# snapshot.debian.org keeps every version forever, so the Debian four
# stay recoverable after the pool moves on. The Raspberry Pi two do
# not, which is why a release has to carry copies of those files.
#
# Sizes are the archive's own, and they are checked as well as the
# hash: a truncated download that stops at a block boundary is a
# thing that happens, and it fails the size check first with a clearer
# message than a hash mismatch gives.
#
# Format: one package per line, "url sha256 size".
PINNED_DEBS="
http://deb.debian.org/debian/pool/main/z/zbar/libzbar0t64_0.23.93-8_arm64.deb 86bf2db996e828f87d2ab94509cd0b4fbc097e57e8976cb6142ee541b2638787 121644
http://deb.debian.org/debian/pool/main/p/pillow/python3-pil_11.1.0-5%2bdeb13u4_arm64.deb ad294f2932ceb9115eece925337b19a6beae3164be9ebb7a4d80057c472fd788 495140
http://deb.debian.org/debian/pool/main/r/rpi.gpio/python3-rpi.gpio_0.7.1%7ea4-1.2_arm64.deb 8d9c1069a4c78ba0f74d84368b37bb9bd23e5ea7e0b0700b4247e85fec2a2137 22292
http://deb.debian.org/debian/pool/main/s/spidev/python3-spidev_3.6-1%2bb6_arm64.deb eb65bc09fc45cc76a612d1581cd29a2084f9e4b90926a0b2257f6e216303c1f1 14380
http://archive.raspberrypi.com/debian/pool/main/p/picamera2/python3-picamera2_0.3.37-1_all.deb ce409345b92f3aeac7c72d8a67ee39935ab07617afd63089e7c10a7383100953 120246
http://archive.raspberrypi.com/debian/pool/main/p/python-pip/python3-pip_25.1.1%2bdfsg-1%2brpt1_all.deb 5ca866e705e1b318701697e3adabcf7ebd35d6540dbbd4fa87bbca57546c1c0f 2266054
"
# Read off the board on 2026-09-23 with `apt-get download --print-uris`,
# which is apt's own answer to "where would you get this and what
# should it hash to". The Raspberry Pi archive publishes SHA512 in its
# index where Debian publishes SHA256, so those two were downloaded and
# hashed rather than copied: both matched their SHA512 entries.

DEV_IMAGE_SHA256="RECORDED_AFTER_PROVISION"     # sha256 of the provisioned SD image (A-16)
